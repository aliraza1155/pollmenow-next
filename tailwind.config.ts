import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: '#6C5CE7',
        secondary: '#a855f7',
        accent: '#ec4899',
      },
    },
  },
  plugins: [],
};

export default config;